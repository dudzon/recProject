import { colors } from "./colors"
import { fonts } from "./fonts"
const theme = {
  //  Colors
  colors,

  //   Fonts
  fonts,

  flexWrapper: () => ({
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  }),
}

export default theme
